<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head', {title: 'Actores'}) %> 

<body>
    <%- include('partials/header') %> 
    <h1>LISTADO DE ACTORES</h1>
    <ul>
        <% for( let i = 0; i < actors.length; i++ ) { %>
        <li>
            <a href="/actors/actors/detail/<%= actors[i].id %>"><%= actors[i].first_name %> <%= actors[i].last_name %></a>
        </li>
        <% } %>

    </ul>
</body>

</html>