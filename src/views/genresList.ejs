<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head', {title: 'Lista de Géneros'}) %>
  <body>
    <%- include('partials/header') %>
    <main class="genres_list_main">
      <section class="genres_list_container">
        <h1 class="titulo_general">LISTADO DE GENEROS</h1>
        <div class="list_peliculas">
        <ul class="movie_box ">
            <% for( let i = 0; i < genres.length; i++ ) { %>
            <li>
              <a href="/genres/genres/detail/<%= genres[i].id %>"
                ><%= genres[i].name %></a
              >
            </li>
            <% } %>
        </ul>
        </div>
      </section>
    </main>
  </body>
</html>
