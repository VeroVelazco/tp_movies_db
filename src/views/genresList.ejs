<!DOCTYPE html>
<html lang="en">

    <%- include('./partials/head', {title: 'Lista de Géneros'}) %> 

<body>
    <%- include('partials/header') %> 
    <h1>LISTADO DE GENEROS</h1>
    <ul>
        <% for( let i = 0; i < genres.length; i++ ) { %>
        <li>
            <a href="/genres/genres/detail/<%= genres[i].id %>"><%= genres[i].name %></a>
        </li>
        <% } %>

    </ul>
</body>

</html>